{
  "servers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--user-data-dir",
        "${workspaceFolder}/.playwright-mcp-profile",
        "--headless"
      ]
    }
  }
}
